-- ThirstUI.client.lua
-- DEPRECATED — the thirst bar has been folded into HungerUI.client.lua
-- as the right half of the combined hunger+water bar. The mana bar now
-- occupies this slot in the left HUD stack via ManaUI/ManaUI.client.lua.
--
-- This file is intentionally left as a no-op stub so any existing
-- reference in the Studio .rbxl does not spawn a second, stale thirst
-- bar on top of the new combined bar. Nothing to do at runtime.
return
